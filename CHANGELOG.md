# 淋浴 (LinYu) 更新日志

---

## v3.0.4

发布日期：2026-09-20

### 版本信息

| 项 | 值 |
|---|---|
| 版本号 | 3.0.4 |
| versionCode | 14 |
| 包名 | `com.hualala.linyu` |
| 构建类型 | Release（R8 混淆 + 签名） |
| 安装包大小 | 12.6 MB |
| GitHub Release | https://github.com/yehu-imei/linyu/releases/tag/v3.0.4 |
| Gitee Release | https://gitee.com/yehu-imei/linyu/releases/tag/v3.0.4 |

---

**本次为质量修复版本，无新增功能。** 改动集中在异常路径与工程加固——正常使用下的行为与 v3.0.3 一致。

### 修复：关阀结果与本地状态

**问题**：`closeValve` 轮询确认关阀结果时，无论结果如何，循环结束后都会执行
`clearDeviceState()` 并返回 `Closed`。更严重的是那次"确认"本身是空的——
它查的 `closeOrder/result/query` 实测**恒返回 `data: null`**（见 [API-qzxy.md](API-qzxy.md) 4.4），
判据里的 `d == null` 永远成立，第 1 轮就 break。也就是说它只验证了「HTTP 请求成功」，
断网导致 5 次请求全部抛异常时，照样会向用户报「使用结束」。

**改法**：

- 新增 `CloseOutcome.Unconfirmed` 状态。关阀请求本身抛出异常、且服务端状态也查不到时返回它，
  调用方据此如实提示，不再谎报成功
- 判据是「**一点证据都没有**」，而不是「确认得很完美」——后者会让网络稍有抖动就报未确认，
  正常停止天天弹「没确认到」，那种噪声会淹没真正的异常
- 停止流程中「本地状态先清」的顺序**保留不动**（那是为了用户点完立刻有反馈），
  改为在未确认时**回滚**：`ShowerController.restoreActiveOrder()` 把活跃订单与
  `startedAt` 一并放回。`startedAt` 是关键——少了它，即使状态补回来，计时也会从 0 重来
- 服务与 App 的本地副本通过 `ShowerEvents.ordersRestored` 对齐：回滚发生在
  `notifyFinished` **之后**，此时 App 已把设备从内存列表删除，不同步会表现为「闪一下空闲、又变回使用中」

### 修复：MQTT 回填订单号

`handleMqttMessage` 里 `showerSnCode?.let { updateOrderNo(it, it) }` 的**内层 `it` 遮蔽了外层**，
两个参数都变成设备序列号，持久化的 `orderNo` 被写成 `snCode`。后果是关阀与结算拿错误的订单号去查，
静默退化成按时间窗猜账单。已改为具名变量。

### 修复：桌面小组件的状态与反馈

| 问题 | 根因 | 改法 |
|---|---|---|
| 「占用中」永久残留 | `occupiedSnCode` 全项目只有小组件自己读写，App 从未清除；且无过期机制 | App 在 `refreshDeviceStatus` 确认设备空闲/`isOwner` 时清除；`occupiedSnCode` 增加 **3 分钟** TTL 兜底 |
| 断网点开启无任何反馈 | `openValve` 第一步即发网络请求且自身无 try/catch，异常冒到接收器只落一条日志 | 接住并转成「网络异常」卡片提示 + 横幅通知（与开阀失败同一套反馈） |
| 「选用」失败文案笼统 | `pickDevice` 返回 `Boolean`，「设备不存在」与「网络不通」无法区分 | 改为三态 `PickResult`，分别提示「设备不存在」/「网络异常」 |
| 重复被拒反复弹横幅 | `showOccupied` 固定 `alert = true` | 同一占用周期内仅首次弹横幅，后续静默更新同一条通知 |

⚠️ 「状态未知」状态下的按钮发的是 `ACTION_REFRESH`（走 `reconcile`，只重新查询服务端状态），
**不会重新开阀**——第一次的 `downRate` 可能仍在服务端飞行，此时重开会造成重复下单。
因此该状态文案为「点此刷新」而非「请重试」。

### 安全

- **应用内更新校验**：新增包名比对、签名证书 SHA-256 比对，以及可选的 Release SHA-256 校验。
  签名与包名不匹配、或签名无法读取的安装包会被**阻止安装**。
  GitHub 的 `digest` 字段不可用时不阻止安装（否则 Gitee 用户无法更新），但签名校验不跳过
- **认证数据备份**：`backup_rules.xml` / `data_extraction_rules.xml` 此前是 Android 模板原样
  （内容全部注释），等于未排除任何内容。现排除 `linyu_prefs.xml`、`linyu_prefs_plain.xml` 与日志文件
- **加密存储降级**：`EncryptedSharedPreferences` 初始化失败时的明文回退改用独立文件
  `linyu_prefs_plain`。此前两者同名，加密恢复后会读到格式不符的同名文件，表现为「登录态莫名丢失」
- **日志并发安全**：`AppLogger` 的共享 `SimpleDateFormat`（非线程安全）改用 `java.time.DateTimeFormatter`；
  文件截断与追加共用同一把锁。脱敏逻辑未改动

### 其他

- **更新包下载**：支持复用已下载且校验通过的文件，避免重复下载；下载成功后清理历史版本残留
- **蓝牙扫描**：保存并撤销超时 `Runnable`，增加一次性回调标志。此前扫描失败后定时任务仍会执行，
  上层会把失败后的空列表当成有效快照写入小组件
- **Release 签名配置**：`local.properties` 缺失或字段不全时不再创建 release signingConfig，
  仅输出警告。此前在**配置阶段**无条件读取该文件，导致干净环境连 `testDebugUnitTest` 都无法执行
- **文档**：`SECURITY.md` 第 6 节此前称「完整的请求/响应体日志只在 Debug 构建里存在」，
  该表述仅对 `HttpLoggingInterceptor` 成立——项目自身的拦截器在 Release 下同样记录
  最多 300/400 字符的请求/响应片段（已脱敏）。已如实修订

---

## v3.0.3

发布日期：2026-09-18

### 版本信息

| 项 | 值 |
|---|---|
| 版本号 | 3.0.3 |
| versionCode | 13 |
| 包名 | `com.hualala.linyu` |
| 构建类型 | Release（R8 混淆 + 签名） |
| 安装包大小 | 12.6 MB |
| GitHub Release | https://github.com/yehu-imei/linyu/releases/tag/v3.0.3 |
| Gitee Release | https://gitee.com/yehu-imei/linyu/releases/tag/v3.0.3 |

---

### 修复：结束用水拿不到消费金额

结束通知几乎总是显示「无消费」，哪怕真用了热水。两个 bug 叠在一起：

1. **拿去比对的根本不是同一个号。** 账单里记的 `orderNo` 是 20 位（`13202609181506275230`），
   而代码拿 7 位的账单序号 `orderId`（`9564403`）去比，永远不成立。
2. **兜底的时间窗把本次这一单滤掉了。** 过滤条件是「账单时间 ≥ 开阀时间」，但账单里的
   `consumeDate` 记的是**下单**那一刻，比开阀确认成功早 1~2 秒——于是本单自己被排除。
   实测日志里 29 次会话全是「消费 0.0」。

改法是不再拉整个账单列表手工对账，直接问服务端「这一单结算了多少钱」：

```
POST /order/consumeOrder/result/query   snCode + orderNo
```

**单位是个坑**：这个接口返回的 `consumeMoney` 是**厘**（1 元 = 1000 厘），
账单列表里同名字段返回的是**元**。字段名一样、类型还一个是数字一个是字符串。
没换算就会**静默**错 1000 倍——用了 0.04 元，通知上写「消费 ¥40.00」。
两笔独立订单用 `dealDate` 对齐验证过比值精确是 1000。

同时把「没查到」和「确实没花钱」分开：以前两者都显示「无消费」，
现在前者是「结算中」、后者才是「无消费」。零消费的单子（开完水马上停）
1~2 秒出结果，以前要 5 轮 + 3 轮固定重试，白等 8.4 秒。

通知的行为也改了：先显示「用时 X · 结算中…」，**用同一个通知 id**，
拿到金额后原地更新成「用时 X · 消费 ¥0.xx」，不会先蹦一条再蹦一条。

### 修复：绑定寝室（issue #6）

「选择附近」列出来的名字是**拼**出来的——设备名最后一段如果全是数字，
就自动补一个「房」字。金华这边设备名结尾本来就带「房」
（`热水表-龙川北苑-3号楼南-3层-320房`），所以一直没暴露；别的学校结尾是 `829`，
补完变成 `829房`，拿去匹配真实设备名一条都命不中，**首页设备列表直接空了**。

改成从设备名「掐头去尾」取寝室标识，不拼任何字：

```
热水表-龙川北苑-3号楼南-3层-320房        →  龙川北苑-3号楼南-3层-320
洗手台54-龙川北苑-3号楼南-3层-320洗手台   →  龙川北苑-3号楼南-3层-320
```

同一间寝室的两台设备收敛成同一条，而且**跟这次扫到哪一台无关**——
以前只扫到洗手台和只扫到热水表，列出来和存下来的东西是不一样的。
键里带着楼栋和楼层，所以 `3号楼南-3层-320` 和 `3号楼北-3层-320` 也不会串。

**桌面小组件以前完全没读取绑定寝室**：显示的是「上次开过阀的设备」，
开阀动作也不看绑定，桌面上照样能开到别的寝室的水。现在主屏、附近设备页、
开阀动作三处都会按寝室筛。两个安全点：**用水中的卡片不受筛选影响**
（那是唯一的停止入口，藏了就只能进 App 停）；provider 的闸门**只拦开阀**，
停止和刷新一律放行（不然等于让人关不了水）。

绑定寝室后，如果「上次使用的设备」不在这个寝室里会被**清掉**（正在用水的除外）——
`lastDeviceSnCode` 是小组件主按钮的开阀依据，留着它就等于留着一条
「点一下就开别寝室的水」的路。手工输入关键词的输入框去掉了，
只能从「选择附近」选：手工存进去的是没和设备名核对过的字符串，
筛空的时候用户分不清是输错了还是这层真没设备。

向后兼容：老的绑定值（`320房`、完整设备名）仍然有效——
匹配规则是「键相等 **或** 键以 `-`+绑定值结尾」。加那个 `-` 是为了不误伤，
`…-1320` 不以 `-320` 结尾，用 `contains` 就会误匹配。

### 界面

- **设备详情卡片**里沐浴/洗漱前面的图标换回 emoji。上一版换成了白色图标，
  但那处**没有底色**（`DeviceGlyph` 的 tint 写死白色，因为它默认压在设备类型色的圆底上），
  白图标压在浅色背景上几乎看不见
- **花洒图标**下面那条水流线加长，和上面那条等长。改的是 SVG 源图，
  包裹盒和缩放系数一字未动，头和中间那条线一个像素都没动
- **提示气泡**改用 `TailEllipsisText`：单行不换行，放不下时先缩字号、再省略开头

---

## v3.0.2

发布日期：2026-09-18

### 版本信息

| 项 | 值 |
|---|---|
| 版本号 | 3.0.2 |
| versionCode | 12 |
| 包名 | `com.hualala.linyu` |
| 构建类型 | Release（R8 混淆 + 签名） |
| 安装包大小 | 12.6 MB |
| GitHub Release | https://github.com/yehu-imei/linyu/releases/tag/v3.0.2 |
| Gitee Release | https://gitee.com/yehu-imei/linyu/releases/tag/v3.0.2 |

---

### 新增：设备图标

🚿 / 🪥 从 emoji 换成单色 vector 图标，用在**主视觉位**：主界面设备卡片、使用中设备卡片、
上次使用卡片、使用页、设备详情弹窗。

**数据层一个字节没动。** 设备类型在全链路是 emoji 字符串（`DeviceInfo.typeEmoji` → 写进
`PrefsHelper.lastDeviceEmoji` → 序列化进小组件的 `widgetBillJson` / `widgetNearbyJson`）。
直接改字面量会让**老用户设备上存着的旧值认不出来**，所以只在渲染时映射：见 `ui/DeviceGlyph.kt`
的 `deviceIconRes()`。以后换饮水机那三个，在那张表里加两行即可。

> 小组件的账单行 / 附近设备行**不换**——那两个列表里可能有饮水机，换了会图标和 emoji 混排。

源图和转换脚本放在 `design/device-icons/`。转换踩过的坑（尤其是**圆弧包围盒**：
只记端点会算小、内容被裁；扩半径或取椭圆又太松、图标被压小）都记在那份 README 里。

### 新增：开阀失败的三处反馈 + 横幅通知

`showerError` 以前是**只写不读**的——开阀失败时用户只看到「正在开启热水器…」消失，
然后什么都不发生。现在三处联动：

| 位置 | 表现 |
|---|---|
| App 弹窗 | 服务端原话 + 有欠费时列出账单和「立即补扣」 |
| 桌面小组件 | 卡片闪 3 秒「开阀失败」（完整原因放不下）|
| 系统通知 | **横幅**带出服务端原话 |

横幅要 `IMPORTANCE_HIGH`，而之前三条渠道最高才 `DEFAULT`（只响一声、不弹），
**所以全 App 一个横幅都没有**。新增 `linyu_alert` / `linyu_alert_quiet` 两条渠道二选一。

> ⚠️ 渠道的 importance **创建后改不了**（系统不允许 App 改已存在渠道），
> 所以「关掉横幅」只能另建一条 DEFAULT 的来切，不能改原来那条。

「我的 → 通知」新增「横幅提醒」开关。

### 修复：代扣的一批重复扣款隐患

这是**从校园卡真扣钱**的路径，全项目审查后集中修了一轮：

| 问题 | 修法 |
|---|---|
| 请求超时/被中断当成「失败」→ 按钮恢复 → 用户重试 → **可能扣两次** | 新增 `DeductResult` **三态**：`Ok` / `Failed`（服务端明确拒绝，可放心重试）/ `Unknown`（没拿到答复，**不能重试**）。`CancellationException` 先于 `Exception` 捕获并重抛——它是 `Exception` 的子类，混进普通 catch 就会被误判成失败 |
| 「结果未知」时靠猜 | 重查一次服务端，**以它为准**：账单还在说明没扣成，不在了说明扣成了。这是唯一能确定答案的办法 |
| 未支付列表无乱序守卫 → 旧响应把扣掉的账单「复活」→ 诱导重复点击 | 加**单调序号**，只接受最新一次响应。`reconcileUnpaid` 也要推序号，否则它刚确权的结论会被在途的旧响应覆盖 |
| 批量补扣没去重（而 `unpaidConsumeDates` 去了）| 统一走同一份口径 |

### 修复：欠费提示的误导与状态残留

| 问题 | 修法 |
|---|---|
| 首页从不加载未支付列表 → 冷启动开阀失败时补扣入口不出现 | 首页 `LaunchedEffect` 里也拉一次 |
| 弹窗不看失败原因，一律断言「有欠费」→ **WiFi 掉线也引导去扣钱** | 新增 `ShowerErrorKind`，**只有 `SERVER_REJECTED`** 才显示欠费块 |
| 弹窗按未过滤的列表报笔数/金额，实扣只处理有日期的 | 统一用 `deductibleBills`；全无日期时弹窗根本不出现（以前会报一次**假成功**）|
| 金额解析失败 fallback 到 `0.0` → 用户对着假数字点确认 | 显示「金额无法识别」+ **禁用确认按钮** |
| 锁命中时静默 `return`，调用方已关弹窗 | 所有早退路径都回调（`BUSY_MESSAGE` / `null`）|
| `logout()` / `kickOut()` 不清 `unpaidBills` 和代扣锁 | 都清。不清的话换账号会展示上一任的账单；锁更麻烦——扣款协程若在启动前就被取消，清锁那行永远跑不到，之后所有代扣静默失效，只能杀进程 |
| 未支付列表在 `forEach` 里逐条读计算属性 | 循环外取一次，避免每个 item 重建一次 Set |

### 修复：使用码

| 问题 | 原因 |
|---|---|
| 点「重新领取」后待领码显示成八个短横线、确定按钮点不了 | `LaunchedEffect` 的条件写成了「只在**手上没有码**时才自动换一个」。有码的用户走进另一个分支，弹窗里就是空的。改成打开就换——用户点这个按钮的意图本来就明摆着 |
| 待领码**整串都是蓝色**，而不是只有后三位 | 卡片上做了「后三位高亮」，弹窗漏了。后三位是手机号后三位**固定的**，每次「换一个」换的只有前五位，整串一个颜色根本看不出变化 |

> 打开弹窗就会消耗一次当日额度（每天 20 次）——要给你看码就得跟服务端要一个，官方也是这个行为。

### 其他

- 小组件「开阀失败」提示期间按钮**恢复可点**（以前 `actionIntent` 对所有 disabled 都返回 null，
  1x1 整块瓷砖 3 秒无响应，用户想重试像小组件坏了）。新增 `DisabledReason.isNotice` 区分
  「过程」和「结果」
- 两套代扣确认弹窗合并成 `DeductConfirmDialog`——它们已经开始漂移了，同一句免责文案
  一度在两边写成「可能扣款失败」和「会扣款失败」。**动钱的界面**两处说法不一致会影响
  用户对风险的判断，所以文案一律写死在一个组件里
- 四条事件通知并进一个 `postEvent`，渠道和优先级**一次算出来**（以前靠字符串把渠道反推回
  优先级，漏一个就静默不弹横幅）
- `deductAllUnpaid` 复用单笔的 `deductOnce`，**顺带找回了批量路径丢失的服务端失败原因**
- 清掉三处死代码：`WidgetState.emoji` 和 `bindHeader` 的 emoji 参数（**收了从来没用过**，
  小组件头部根本没有 emoji 控件）、`ShowerController.lastDeviceEmoji()`

### 已知限制

- **仅支持 64 位（arm64-v8a）设备**
- 小组件的设备头像仍是 emoji（账单行 / 附近设备行）
- 小组件不连 MQTT：使用中看不到实时消费金额，只显示预扣
- 附近设备 / 账单两页是 **App 上次同步的快照**，小组件自己扫不了蓝牙也拉不了账单

---

## v3.0.1

发布日期：2026-09-17

### 版本信息

| 项 | 值 |
|---|---|
| 版本号 | 3.0.1 |
| versionCode | 11 |
| 包名 | `com.hualala.linyu` |
| 构建类型 | Release（R8 混淆 + 签名） |
| 安装包大小 | 12.5 MB |
| GitHub Release | https://github.com/yehu-imei/linyu/releases/tag/v3.0.1 |
| Gitee Release | https://gitee.com/yehu-imei/linyu/releases/tag/v3.0.1 |

---

### 新增：残留账单的手动请求代扣

12 点后结束用水、或者一卡通余额不足时，服务端结算没扣成，账单会留在待扣状态。官方 App 里点账单详情能手动补扣，现在这边也能了。

接口是 **`POST /order/thirdTrade/single/json/pay`**：

```json
{"deductType":7,"consumeDate":"2026-09-16 21:03:17",
 "projectId":"905","accountId":"49675","loginCode":"...","userId":"19679297","phoneSystem":"WeChat"}
```

它和本项目**其他所有 POST 都不一样**，照抄现有写法会失败：

| | 其他 POST | 这个 |
|---|---|---|
| body | form-urlencoded | **JSON** |
| 账单标识 | `orderNo` | **`consumeDate`** |

**哪些账单显示按钮由服务端说了算**——读 `POST /order/weixinScorePay/unPay/queryBill` 的未支付列表，不去猜账单里 `consumeMoneyReal` / `upState` 那些字段的含义（它们看着像，但都是猜的）。

> ⚠️ **这是动钱的接口**，防重是第一要务：请求进行中直接拒绝新请求、按钮置灰且文案变「扣款中…」、成功后立刻把这条从未支付集合里摘掉（不等网络往返——万一刷新失败按钮还挂着，用户会以为没扣成功又点一次）。另外点按钮先弹确认框显示金额。

**实测验证（2026-09-18）**：残留账单代扣成功，请求体与实现逐字段一致。`deductType: 7` 有两个独立样本（一次服务端故障时的失败、一次成功），`phoneSystem: "WeChat"` 实测可成——这个接口是官方 App 里那个 H5 页面调的，和 Android 原生请求（`phoneSystem=android`）不是一条路。

> 💡 当晚 00:02 首次测试失败，返回 `errorCode: -1 账户异常`。**不是代码问题**：同一时刻 `/settlement/campus/userInfo` 也返回 `errorCode: -1 接口请求异常`，整个一卡通子系统在报错（疑似日终维护）。次日同一接口正常后，代扣立刻成功。

### 新增：1x1 小组件

第三种尺寸，**一格大小，点一下开关热水**。

| 状态 | 顶部小字 | 底色 | 图标 | 点击 |
|---|---|---|---|---|
| 空闲 | 空闲 | 玻璃灰 | 电源 | 开阀 |
| 使用中 | 使用中 | 蓝 | 停止方块 | 关阀 |
| 他人使用中 | 占用中 | 橙 | 锁 | 仍是开阀，服务端拒绝后弹占用提示 |
| 操作中 | （不写字） | 玻璃灰 | 转圈 | 不响应 |

几个实现上的取舍：

- **状态不只用颜色区分**。色弱的用户、或者瞟一眼的时候，光靠蓝/橙分不出「空闲」和「使用中」，所以顶部加了一行 8sp 的小字。
- **「空闲」那块是三种情况共用的**（真·空闲 / 未登录 / 没选过设备），文字动态换——未登录时写「空闲」会让人以为点一下就能开阀。
- **转圈用系统的 `ProgressBar`**。RemoteViews 跑不了动画，但 `ProgressBar` 的不确定态是**系统进程画的**，在桌面上照样转。开阀要 5 秒、关阀加结算 12 秒以上，这期间原本什么反馈都没有。
- **「他人使用中」和「切换失败」不转圈**——它们是**结果**不是过程，转圈会误导。
- **对齐了应用图标的视觉大小**。`minWidth/minHeight` 不是"组件多大"而是"至少需要多大"，桌面据此决定给几格、然后把组件**拉伸填满那几格**，所以不加内边距时玻璃块会顶到格子边缘、比图标大一圈。加了 6dp 内边距补这个差值。
  - 留白区域落在根布局上、不属于任何一块 tile，所以根布局也挂了一份当前动作，否则边缘会变成**看得见点不着**的死区
  - ⚠️ 各家桌面的格子尺寸和图标内边距都不一样（小米 / 华为 / 原生三套参数），做不到像素级一致，6dp 是折中值

### 修复：姓名拿不到

原来只有 `/account/info` 一个来源，而它的 `name` 要**学校同步学籍数据**才有——没同步的账号返回的是 `null`，那一行就直接不显示了。

现在三个来源汇总，谁有值用谁的：

| 接口 | 字段 |
|---|---|
| `/account/info` | `name` / `idCardNumber` |
| `/account/card/getBindCardInfo`（**新加**） | `name` / `idCardNumber` |
| `/settlement/campus/userInfo` | `studentName` / `studentNumber` |

后两个的数据来源不同，一个没有另一个可能有。另外之前从一卡通接口**只取了学号、把姓名漏了**，这次一并补上。

统一收口到 `applyIdentity()`，同时写 Prefs **和** `accountInfo` 这个 State——只写 Prefs 不会触发重组，数据回来了界面也不刷新。

### 问题修复

| 问题 | 原因 |
|---|---|
| 首页「使用中的设备」名字长了换行，把卡片撑变形 | 用的是普通 `Text`，没有 `maxLines`。改用 `TailEllipsisText`——它放不下时**省开头、保结尾**（组件名里的 `Tail` 指保留尾部），而系统默认的 `TextOverflow.Ellipsis` 正好相反，砍掉的正是有辨识度的房号 |
| 点「正在使用」通知回到的是首页 | 通知的点击意图写的是 `tab = 0`。现在带一个 extra，MainActivity 收到后把使用页拉回来（覆盖「已在使用页 / 点过最小化 / App 被杀过」三种情况） |
| 通知权限提示的黄色块没占满一行 | `Surface` 默认**按内容撑宽**，少了 `fillMaxWidth()` |
| 关掉通知总开关后仍提示「请打开通知权限」 | 判断条件漏了总开关。用户本来就是不想要通知，再弹这个还配一个能一键打开系统权限的按钮，等于跟他对着干 |

### 其他改进

- 通知权限提示文案改成「请打开通知权限」
- 「设备被他人占用」的通知：标题改成「设备占用中」、设备名挪到正文。设备名很长（「龙川北苑 3号楼南 3层 320房」），放在标题里会把通知栏那一行占满，一眼看不出是**发生了什么**
- 1x1 小组件的预览图换成专门画的静态图，并去掉了 `previewLayout`（那个会让 API 31+ 的桌面无视 `previewImage`）

### 已知限制

- **仅支持 64 位（arm64-v8a）设备**
- 1x1 小组件占用仍然是**一格**（和应用图标一样），只是靠内边距让视觉大小接近；像素级对齐做不到，各家桌面参数不同
- 小组件不连 MQTT：使用中看不到实时消费金额，只显示预扣
- 附近设备 / 账单两页是 **App 上次同步的快照**，小组件自己扫不了蓝牙也拉不了账单

---

## v3.0.0

发布日期：2026-09-17

### 版本信息

| 项 | 值 |
|---|---|
| 版本号 | 3.0.0 |
| versionCode | 10 |
| 包名 | `com.hualala.linyu` |
| 构建类型 | Release（R8 混淆 + 签名） |
| 安装包大小 | 12.5 MB |
| GitHub Release | https://github.com/yehu-imei/linyu/releases/tag/v3.0.0 |
| Gitee Release | https://gitee.com/yehu-imei/linyu/releases/tag/v3.0.0 |

---

### 新增：一卡通真实余额

**这是 2.x 以来被问得最多的一个限制，现在解掉了。**

原先只能让用户手填一个初始金额，再拿账单往下减——算得再准也只是估算。现在直接读一卡通账户：

```
GET /settlement/campus/userInfo  →  {"amount":"16.76","signStatus":1}
```

趣智校园把易校园的接口代理了，所以**不用去破易校园那套 HMAC-SHA256 的 native 签名**。

| 情况 | 界面表现 |
|---|---|
| 已签约免密支付 | 显示真实余额，副标题「来自校园卡账户的实时余额」 |
| 未签约 / 接口失败 | 回退「初始余额 − 账单消费」的估算，副标题注明「并非真实余额」 |

拿到真实余额后「手动填写」按钮会隐藏（估算值会被真实值覆盖，摆在那儿就是误导），桌面小组件的标签也从「一卡通余额（估算）」变成「一卡通余额」。

> ⚠️ 这个接口**出错也返回 HTTP 200**，错误信息在 `errorMessage` 里。一开始就是被这点骗过去的——状态码正常、`data` 是 null，代码静默回退到估算，看起来像「功能没生效」。实际原因是 GET 拦截器少补了一个 `telPhone` 参数。

### 新增：后台用水监控（前台服务）

原来「超时自动关停」的倒计时和「每 15 秒查一次订单」这两件事写在 `MainViewModel.timerJob` 里，而这个 Job：

- 在 `minimizeShower()` 里被 cancel —— **退出洗澡页就停摆**
- 只在 `startShower()` 里启动 —— **从小组件开阀时压根不会启动**

结果就是退出使用页、或者从桌面小组件开的水，超时了也没人管，热水器一直开着。

搬到 `ShowerWatchService`（前台服务）之后，监控跟界面彻底解耦。判定逻辑只有一个原则：**以服务端还有没有这个订单为准**，网络失败时什么都不做，绝不因为一次抖动就误判结束。

### 新增：系统通知

三条通知渠道，按「要不要打扰人」分开：

| 渠道 | 优先级 | 用途 |
|---|---|---|
| 用水状态 | LOW | 用水期间常驻，系统自己走秒，不响铃不震动 |
| 用水状态（静默） | MIN | 关掉「用水状态通知」后折叠到这里 |
| 用水提醒 | DEFAULT | 结束、超时自动关停、设备被占用 |

常驻通知带一个「结束用水」按钮，不用再回 App。所有通知都由「我的 → 通知」里的开关控制，一个总开关 + 三个细分开关。

> **关掉开关的语义是「把它收起来」，不是「关掉后台监控」。** Android 强制要求前台服务必须挂一条通知，没有就起不来——而前台服务正是自动关停的唯一保障。所以关掉时改用 MIN 优先级的静默渠道，用户看不到，服务照常跑。

### 新增：个人信息卡片

- 姓名、学号走 `/account/info`，学校名走 `/project/info/triple` 的 `projectName`——**都不用手填**
- 拿不到的字段整行不显示，不写「未设置」
- 底部「账号管理」折叠区：更换手机号、修改密码

### 新增：修改密码的短信验证分支

`/user/password/update` 必须带 `oldPassword`，而**用验证码注册的账号根本没有旧密码**——对他们来说「修改密码」原本是个死按钮。

抓包找到了另一条路：

```
GET  /user/verification/code/get?...&typeId=2&telephone=<当前手机号>   ← 不需要旧密码
POST /user/password/forget   password=<新密码>&code=<验证码>
```

弹窗里点「短信验证修改密码」就切到这条，手机号预填当前账号（可改）。

### 新增：使用码改版

服务端的三个接口分工是：

| 接口 | 干什么 |
|---|---|
| `GET /account/useCode/new` | 拿当前生效的码 |
| `POST .../generate` | **换一个**——只返回候选码，不生效，每天 20 次 |
| `POST .../set` | **确定领取**——唯一会改当前码的一步 |

抓包确认了关键一点：**连换 7 次码期间，服务端生效的码一直没变，直到 `set` 才变。** 所以「重新领取」可以做成预览式的：

- 点「领取使用码」/「重新领取」→ 弹窗预览候选码
- 「换一个」反复换，显示今日剩余次数
- 换出来的码有 **3 分钟领取时限**，超时作废
- **取消是零成本的**，当前码纹丝不动

没领过码的用户现在显示「尚未领取」而不是永远的「加载中」——服务端对这种情况返回的 `useCode` 就是 `null`。

### 新增：扫码界面重写

- **从相册选图识别**（`PickVisualMedia`，不需要存储权限）
- 取景框（四角 + 上下往复的扫动光带）+ 识别成功的震动反馈
- 手电筒按钮重做：开启后变暖黄、图标换成填充版、外圈带光晕
- 权限被拒 / 相机启动失败会给说明卡片，不再黑屏一闪

### 问题修复

| 问题 | 原因 |
|---|---|
| 换手机号后个人信息卡片一直显示旧号 | 手机号存在 MainActivity 的一个 `rememberSaveable` 里，只在登录成功时写一次，改号后没人动它 |
| **通知栏点「结束用水」后使用页不退出** | `doFinish` 漏了发结束事件 |
| **消费金额显示成上一次账单的金额** | `settleAmount` 里的时间过滤条件写反了 |
| **小组件开阀后没有「使用中」通知** | `runCatching { startForegroundService(...) }` 把启动失败的异常吞了。点通知里那条「结束」反而正常，因为结束分支在登录校验之前就先挂了通知 |
| 换手机号时验证码发到旧号上 | 实测是发到**新**号（`typeId=5`），代码写反了，用户永远收不到 |
| 部分机型登录页输入框文字发灰 | 登录卡片是固定白底，但输入框取的是主题的 `onSurface`，深色模式下是浅色 |
| 深色模式下导航栏按钮不跟随自定义主题色 | 写死成了 `#A8C7FA`，注释还写着「跟随强调色」 |
| 余额的「手动填写」把卡片行高撑开 | `TextButton` 自带 48dp 最小高度 |
| 「选择附近」同一间房出现两条 | `320洗手台` 和 `320卫生间` 各自截最后一段。现在剥掉设备类型词，都收敛成 `320房` |
| 从小组件进账单页会闪估算余额和「手动填写」 | 新 ViewModel 的 `campusBalance` 初值是 null，现在从 Prefs 恢复 |
| 小组件账单页余额不更新 | 小组件是渲染时读 Prefs 的，拿到真实余额后没推刷新 |
| 扫码页顶部按钮贴到状态栏 | 布局间距写的是裸 px（`topMargin = 24`，3x 屏上只有 8dp），而同一个文件里按钮尺寸却乘了 density |
| 每帧重新创建二维码识别模型 | `BarcodeScanning.getClient()` 写在分析回调里，每帧新建再 close |

### 其他改进

- 登录页默认**手机号验证码登录**（未注册的手机号会自动创建账号）
- 扫码页改用 Compose 重写，间距一律 dp，安全区统一处理
- 「我的」页面卡片默认顺序：账号信息 → 使用码 → 通知 → 背景装扮 → 绑定寝室 → 软件更新 → 关于项目 → 运行日志
- 「账号管理」「领取使用码」默认折叠

### 已知限制

- **仅支持 64 位（arm64-v8a）设备**
- 小组件不连 MQTT：使用中看不到实时消费金额，只显示预扣
- 小组件停止后不做账单结算，回到 App 会正常结算并显示
- 附近设备 / 账单两页是 **App 上次同步的快照**，小组件自己扫不了蓝牙也拉不了账单

---

## v2.2.3

发布日期：2026-09-16

### 版本信息

| 项 | 值 |
|---|---|
| 版本号 | 2.2.3 |
| versionCode | 8 |
| 包名 | `com.hualala.linyu` |
| 构建类型 | Release（R8 混淆 + 签名） |
| 安装包大小 | 12.5 MB |
| GitHub Release | https://github.com/yehu-imei/linyu/releases/tag/v2.2.3 |
| Gitee Release | https://gitee.com/yehu-imei/linyu/releases/tag/v2.2.3 |

### 国内镜像下载

新增下载源开关，**默认走 Gitee**。

| 下载源 | 实测速度 | 12.5 MB 耗时 |
|---|---|---|
| GitHub Release 附件 | 103 KB/s | 约 2 分钟 |
| Gitee 镜像 | 1.90 MB/s | 约 7 秒 |

- 地址**不需要额外请求**：Gitee 的 tag 与附件名和 GitHub 保持一致，直接拼出来即可
- **检查更新仍走 GitHub API**（`api.github.com` 实测 0.6 秒能通），只有下载这一步换了源
- 更新卡片里加了开关，镜像不通时用户能切回 GitHub
- 镜像地址拼不出来时（tag 为空）自动回落 GitHub，不会给出死链

### 问题修复

| 问题 | 原因 |
|---|---|
| 小组件账单页余额偏高 | `cacheBillsForWidget` 只缓存了最近 **2 笔**账单，而 App 的余额用 `billList`（20 笔）算。小组件拿这 2 笔做减法，减数偏小，余额虚高。改为缓存 20 笔（界面仍只渲染 2 行） |
| 扫描失败会清空小组件快照 | `onScanFailed` 和正常超时走的是同一个回调，失败时结果为空，被当成"附近没有设备"写进快照，小组件白白变成「未发现热水器」。给回调加了「是否正常扫完」标志，只有正常扫完才写 |
| 预发布被当成正式版 | `fetchReleases()` 没过滤 `prerelease` 字段，`firstOrNull()` 直接当最新版，发个测试版会提示所有人更新 |
| 小组件「上次消费」不跟设备走 | 消费金额只存了一个全局值，用「选用」换设备后，卡片顶着新设备的名字显示上一台的金额。改为按 `snCode` 分开存（沿用 `startedAt_$snCode` 的前缀模式） |

### 其他改进

- 更新日志默认只列最近 3 个版本，点「展开全部」可以看其余（最多 10 个）。
  之前最多铺 10 个版本，卡片会被拉得很长，**「检查更新」按钮被顶到屏幕外**，
  想更新的人还得先划过一堆旧日志
- 仓库增加 Gitee 镜像，两个仓库互相加了链接

### 已知限制

- **仅支持 64 位（arm64-v8a）设备**
- 小组件不连 MQTT：使用中看不到实时消费金额，只显示预扣
- 小组件停止后不做账单结算，回到 App 会正常结算并显示
- 附近设备 / 账单两页是 **App 上次同步的快照**，小组件自己扫不了蓝牙也拉不了账单
- 余额为估算值（手动填写的初始金额 − 账单消费），并非真实余额

---

## v2.2.2

发布日期：2026-09-16

### 版本信息

| 项 | 值 |
|---|---|
| 版本号 | 2.2.2 |
| versionCode | 7 |
| 包名 | `com.hualala.linyu` |
| 构建类型 | Release（R8 混淆 + 签名） |
| 安装包大小 | 12.5 MB |
| GitHub Release | https://github.com/yehu-imei/linyu/releases/tag/v2.2.2 |

### 包体积优化（42.6 MB → 12.5 MB）

起因是有用户提 issue 说包太大，拆开一看，超过一半的体积跟功能无关。

| 块 | 之前 | 之后 | 说明 |
|---|---|---|---|
| `lib/` native | 19.43 MB | 4.75 MB | 只打包 arm64-v8a |
| `res/` 图片 | 12.50 MB | 0.90 MB | 图标按密度重建 + 调色板 PNG |
| dex | 8.42 MB | 5.42 MB | 移除 material-icons-extended |
| resources.arsc | 1.06 MB | 0.36 MB | 资源语言限定 zh / en |
| assets（ML Kit 模型） | 0.85 MB | 0.85 MB | 不变 |

- **图标**：同一个 1254×1254 的 PNG，在 5 个密度目录 × 3 个文件名下各复制了一份，
  再加一张应用 logo，一共 **16 份完全相同的图**（MD5 一致），占掉 11.6 MB——
  整个包的四分之一还多。其中 mdpi 那份本该是 48×48。现已按各密度重建，并改用 256 色调色板
- **只打包 arm64-v8a**：ML Kit 扫码引擎的 `libbarhopper_v3.so` 每个架构各存一份，
  且在 APK 里是**不压缩存储**的，x86 / x86_64 两份合计 11.5 MB，
  只有模拟器用得到，真机上永远不会加载
- **移除 material-icons-extended**：全项目只用到它里面 2 个图标（扫码、账单），
  但 R8 并没能把它裁掉——发布 dex 里实测残留 **10660 个图标类**。
  那两个图标已换成项目自带的矢量图

> ⚠️ **本版本起只支持 64 位（arm64-v8a）手机**。纯 32 位的老机型装不上。

### 新增功能

- 桌面小组件「选用」改为**在桌面直接切换设备**：点完当场把该设备设成小组件的控制目标
  并切回设备控制页，不再跳回 App 弹设备详情

### 问题修复

| 问题 | 原因 |
|---|---|
| 小组件账单页的余额不跟着消费变化 | 小组件读的是 `manualBalance`，也就是用户当初填的**初始值**本身；而 App 内显示的是「初始值 − 已消费」。这段估算逻辑在主页和账单页各写了一份，小组件是第三份，抄的时候漏了减法。现已抽成 `BalanceEstimator`，三处共用 |
| 小组件附近设备「选用」点 309 却打开 307 | 两行按钮的 `PendingIntent` requestCode 都是 `3000 + appWidgetId`，`FLAG_UPDATE_CURRENT` 让后写入的把前一个覆盖掉，于是两行都指向最后绑定的那个 MAC |
| 重新登录 / 从小组件返回 App 时余额先闪初始值再跳变 | 账单是异步拉的，到位之前减数为 0，界面就把初始余额亮了出来。加了 `billsLoaded` 标记，没加载完显示「—」 |
| 短信验证码按钮点下去要过一会才出倒计时 | `countdown = 60` 写在网络请求**成功之后**。改成点击立刻起一个独立倒计时协程，发送失败则撤销 |

### 体验优化

- 蓝牙扫描时长 10 秒 → 5 秒，刷新重新扫描同样是 5 秒
- 2x2 小组件取消横向圆球布局。原先只要比正方形宽一点（`minWidth > minHeight`）就会切成
  「按钮在右侧」的圆球版，而横向版里的卡片固定 96dp、圆球固定 58dp，都不跟尺寸缩放，
  拉大了只会多出空白。现在任何尺寸都用同一套卡片布局，中间区域靠 `layout_weight` 自适应
- 附近设备页区分「扫过但附近没有」（显示「未发现热水器」，和 App 内一致）和「还没扫过」
- 登录页密码框右侧加一键清空按钮

### 已知限制

- **仅支持 64 位（arm64-v8a）设备**
- 小组件不连 MQTT：使用中看不到实时消费金额，只显示预扣
- 小组件停止后不做账单结算，回到 App 会正常结算并显示
- 附近设备 / 账单两页是 **App 上次同步的快照**，小组件自己扫不了蓝牙也拉不了账单
- 余额为估算值（手动填写的初始金额 − 账单消费），并非真实余额

---

## v2.2.1

发布日期：2026-09-15

### 版本信息

| 项 | 值 |
|---|---|
| 版本号 | 2.2.1 |
| versionCode | 6 |
| 包名 | `com.hualala.linyu` |
| 构建类型 | Release（R8 混淆 + 签名） |
| GitHub Release | https://github.com/yehu-imei/linyu/releases/tag/v2.2.1 |

### 桌面小组件重做

按液态玻璃设计稿重构，并修掉一批实测暴露出来的问题。

#### 视觉

- 换成「**深色中性玻璃 + 白色文字 + 白色高光 + 绿色状态色**」的固定配色
- **不再按壁纸明暗切深浅**：壁纸是深色时，深浅两版玻璃看起来几乎一样，用户完全感觉不到变化，反而困惑
- 玻璃分层（从下到上）：深色半透明底（0.42）→ 对角白光 → 顶部高光 → 白色轮廓
- 文字分三级：主文字纯白 / 次级 0.68 / 弱信息 0.48
- 图标 0.82 白，主操作图标纯白；状态点保留绿（空闲）/ 蓝（使用中）

> 设计稿里的 `backdrop-filter: blur(24px)` 在小组件上**无法实现**——RemoteViews 不能跑着色器，
> 框架也不提供模糊接口。这里靠"加深底色 + 加高光"补足空间感，也正因为没有模糊兜底，
> 透明度才取 0.42 而不是更透：可读性优先。

#### 布局

- 尺寸从 6 套（浅深各半）砍成 **3 套**：2x2 竖向 / 2x2 横向 / 2x4
- 开关按钮**只留图标**（电源 / 停止），去掉文字
- 2x4 头部改成整行铺满、状态徽章贴最右（之前被按钮挤到左边，很难看）
- 附近设备：信号点后补上 **dB 数值**，标题后加「**（x 分钟前）**」标明这是什么时候的数据

#### 功能

- **操作状态全局同步**：点任一个小组件，桌面上所有淋浴小组件会同时进入
 「正在开启…/正在关闭…」，卡片位置显示转圈（系统 ProgressBar，原生动画）
- **卡片点击分状态**：空闲 → 打开 App 的账单页；使用中 → 直接关阀
- 附近设备「选用」→ 打开 App 并直接弹出该设备详情
- 账单页 → 打开 App 的账单页

### 问题修复

| 问题 | 原因 |
|---|---|
| 小组件按钮点了没反应 | PendingIntent 指向了未在 Manifest 注册的基类，广播被系统**静默丢弃**（无异常、无日志） |
| 2x2 拉宽后整个小组件显示「载入窗口小部件时出现问题」 | 横向布局的按钮是 ImageView，渲染时却调了 `setTextViewText`——RemoteViews 反射找不到 `setText` 会抛 ActionException |
| 切到附近设备页必崩 | `CachedDevice` 新增字段后，旧缓存 JSON 缺该字段；Gson **绕过 Kotlin 非空约定**直接写 null，`isNotEmpty()` 内部 NPE |
| 小组件预扣一直显示 ¥0.00 | 小组件开阀时只有 snCode、没有 DeviceInfo，`withholdMoney` 取了默认 0。改为单独持久化一份 |
| 两个小组件的计时每秒错开一小段 | `chronometerBase` 用「当前时刻」反推基准，每次重绘都会平移；Chronometer 按基准的秒边界跳，于是相位错开。改为只由 `startedAt` 决定 |
| 「使用中」徽章在 2x2 上是白的、2x4 上是蓝的 | 改颜色时只改了其中一个布局文件 |
| 附近设备信号点看不出强弱 | 只有一个圆点，没有具体数值 |
| 首页设备名过长时盖到按钮上 | 测量省略号宽度时没算上主题的 `letterSpacing` / `fontFamily`，量出来偏窄，文字实际溢出了容器 |

### 其他改进

- 首页「上次使用的设备」的设备名与 MAC 改为**头部省略**（保留结尾的房号与 MAC 后缀），
 并且**不换行**——系统字体调大时也不会把卡片撑变形；放不下时先缩字号，缩到下限才省略
- 小组件「使用中」徽章、状态点、按钮等元素在三套布局之间颜色统一（补了跨布局一致性检查）
- 清理了 6 个已无引用的旧资源

### 已知限制

- 小组件不连 MQTT：使用中看不到实时消费金额，只显示预扣
- 小组件停止后不做账单结算，回到 App 会正常结算并显示
- 附近设备 / 账单两页是 **App 上次同步的快照**，小组件自己扫不了蓝牙也拉不了账单
- 设计稿的模糊、按压缩放、状态过渡等动画在小组件上均无法实现

---

## v2.2.0

发布日期：2026-09-14

### 版本信息

| 项 | 值 |
|---|---|
| 版本号 | 2.2.0 |
| versionCode | 5 |
| 包名 | `com.hualala.linyu` |
| 构建类型 | Release（R8 混淆 + 签名） |
| GitHub Release | https://github.com/yehu-imei/linyu/releases/tag/v2.2.0 |

### 新增功能

#### 1. 桌面小组件（2x2 / 2x4）

在桌面直接启停热水，不用每次打开 App。

| 形态 | 布局 |
|---|---|
| **2x2 竖向** | 第一行 emoji + 设备名，第二行「上次消费」，计时器紧贴圆形按钮上方，按钮略微偏下 |
| **2x2 横向** | 左侧竖排设备名 / 上次消费 / 计时器，圆形按钮占满右侧高度（拖动拉宽后自动切换） |
| **2x4** | 顶行 emoji + 设备名 + 状态，其余空间由大圆形按钮占据 |

- 样式对齐 App 内卡片：22dp 圆角 + 近白半透明底 + 淡描边（深浅两套）
- 圆形按钮「开」为强调色实心圆、「关」为红色实心圆，并**随可用空间自动放大**
- 「上次消费」金额会持久化（结算成功、或拉到账单列表时写入），小组件只读，不额外发请求
- 计时用 `Chronometer` 实现——它在启动器进程里自己走秒，App 关掉甚至进程被杀都不影响
- App 内启停后桌面会立刻同步；小组件自己操作完也会刷新**桌面上全部**小组件，2x2 和 2x4 同时摆放时状态一致

实现细节：

- 新增 `data/ShowerController.kt`，把开阀 / 关阀 / 查询 / 结算从 `MainViewModel` 抽成共享层，App 与小组件走同一份逻辑
- 小组件全程只用 RemoteViews 一等公民 API（`setTextViewText` / `setViewVisibility` / `setChronometer` / `setOnClickPendingIntent`），不用反射调用，避免 `@RemotableViewMethod` 白名单不通过导致整个小组件渲染失败
- 圆形按钮用 `ImageView` + `scaleType="fitCenter"`：圆形 drawable 里写了 `<size>` 提供固有 1:1 宽高比，因此能随容器等比缩放且始终是正圆。用 TextView 背景的话会被拉成椭圆
- 「开 / 关」两张圆形 drawable 靠 visibility 切换，而不是运行时改背景；按钮文字是叠在圆上的独立 TextView，点击事件需同时挂在圆和文字上
- 2x2 的横竖向由 `onAppWidgetOptionsChanged` 里读实际宽高比决定
- 弹性占位用空 `TextView` 而不是 `Space`——`Space` 不在 RemoteViews 的 View 类型白名单保证范围内

#### 2. 应用内下载更新

- 更新卡片检测到新版本后，可直接在应用内下载并调起系统安装器
- 下载过程显示「已下载 / 总大小」与进度条，可取消
- 完成后的提示引导用户去系统设置允许「安装未知应用」
- 下载入口位于「检查更新」按钮下方，收在同一张卡片内

#### 3. 扫描权限改为按需申请

- 蓝牙扫描权限不再在登录后立刻弹出，改由用户在「附近设备」处点「授予权限并扫描」时申请
- **Android 12 及以上不再需要定位权限**：`BLUETOOTH_SCAN` 声明 `neverForLocation`，只申请「附近的设备」
- Android 8 ~ 11 因系统强制规定仍会要定位权限，但引导卡片里说明了原因
- 也可以完全不授权，直接用扫码绑定设备

#### 4. 登录页接入系统自动填充

- 手机号 / 密码 / 验证码输入框标注了自动填充身份，手机的密码管理器可以保存与回填
- 当前使用的 Compose 1.6 没有官方的 `Modifier.autofill`（该 API 到 Compose 1.8 才有），因此使用底层 `AutofillNode` + `AutofillTree` + `LocalAutofill` 实现

#### 5. 挤号检测改为主动

- 原来只有发请求时才顺带检查挤号，挂在页面不动就永远发现不了
- 新增 25 秒心跳轮询，被挤下线最多 25 秒内弹提示
- 心跳跟随 Activity 生命周期，退到后台自动停止，不额外耗电

#### 6. 关于项目卡片改版

- 显示客户端简介、项目名与 Star 数、制作者、联系方式（点击可发邮件）

### 问题修复

| 问题 | 修复方式 |
|---|---|
| 小组件按钮点了没反应 | PendingIntent 指向了未在 Manifest 注册的基类 `LinYuWidgetProvider`，广播被系统静默丢弃。改为反查 widget id 所属的实际注册组件 |
| 2x2 与 2x4 同时摆放时状态不同步 | 操作完成后只重绘了被点的那个小组件。改为重绘桌面上全部小组件（两者读的是同一份持久化状态） |
| 下载完成后凭空跳出「下载失败 / 网络异常」 | `CancellationException` 是 `Exception` 子类，被笼统的 `catch (e: Exception)` 当成网络失败上报。改为单独前置捕获并原样抛出——取消永远不算失败 |
| 重试下载时新旧任务状态互相覆盖，界面乱跳 | 每次下载分配自增的 attempt 令牌，只有当前任务能写状态，旧任务的回调一律作废 |
| 点「立即安装」没反应、也跳不到授权页 | 文件不存在时原来是静默 `return`；且 `ACTION_MANAGE_UNKNOWN_APP_SOURCES` 在部分厂商 ROM 上不存在，异常被吞掉。现在返回明确的安装结果并显示原因，设置页跳转按「带包名标准页 → 无参标准页 → 应用详情页 → 手动操作指引」四级降级 |
| 下载被截断也当成功，给出一个装不上的包 | 下载完比对字节数，不完整就报错重下 |
| 更新卡片检测到新版本却没有下载入口 | 原先只做检测与展示日志，未解析发行版的 APK 附件地址。现已补全整条链路 |
| 被挤下线后重新登录，刚进去又被弹出来，需要登两次 | 根因是 `checkKick` 只标记状态、没掐断在途请求——旧会话的响应在重新登录之后才回来，把新会话的凭证也清掉了。改为所有请求挂在会话级 `SupervisorJob` 上，挤号 / 退出时整体取消，并新增 `beginSession()` 在登录时重置会话 |
| 退出登录后上一任账号的计时器还在 | `PrefsHelper.clear()` 里写的是 `remove("startedAt_")`，但真实 key 是 `startedAt_<snCode>`，删不掉。改为按前缀扫描删除 |
| 登录页点「发送验证码」输入框宽度会跳一下 | 按钮文案在「发送」↔「60s」间变化会撑宽按钮，挤压左侧输入框。给按钮固定 84dp 宽度 |
| 更新日志里显示 Markdown 星号 | App 用的是普通文本渲染，`**加粗**` 会原样显示。解析时去掉 `**` / `__` / 反引号 |
| 换背景图后画面莫名变暗变糊 | 换图会沿用上一张调过的透明度 / 模糊 / 亮度。改为换图时重置为默认值 |

### 权限变更

- 新增 `REQUEST_INSTALL_PACKAGES`（应用内安装更新包所需）。首次使用时会要求允许「安装未知应用」，需在系统设置里手动开启一次

### 体验优化

- 背景装扮默认参数调整为**透明度 100% / 模糊 0 / 亮度 100%**（即新图不作任何处理）
- 小组件开阀 / 关阀的确认轮询设 6 秒预算，超时显示「状态未知，点此刷新」而不是假装成功；点击会按服务端真实状态对齐
- 更新包的下载区收在更新卡片内、「检查更新」按钮下方，不再有第二个下载入口

### 升级提示

- 从 v2.1.0 升级可直接覆盖安装（versionCode 4 → 5，签名一致）
- 小组件需要在桌面长按 → 小组件 → 找到「淋浴」手动添加，会看到两个尺寸条目

### 已知限制

1. **小组件不连 MQTT**：使用中看不到实时消费金额，只显示预扣值
2. **小组件停止后不做账单结算**：结算需轮询账单最多 7 秒，超出广播预算；回到 App 会正常结算并显示
3. **挤号检测有最多 25 秒延迟**：心跳间隔所限
4. **实时扣费不可得**：服务器不提供中间消费数据，只能在关闭后查最终金额
5. **一卡通余额**：易校园 API 有 HMAC-SHA256 native 签名保护，仍只能手动估算
6. **各校部署有差异**：projectId 由登录响应自动下发，无需配置；但各校的 BLE 广播名 / MQTT 地址 / 接入的设备类型可能不同，多数学校装发行版即可，少数需自行适配
7. **饮水机未实机验证**：作者所在学校无直饮水机，识别与 UI 已实现但控制流程未验证

> 本版本重构了洗澡核心流程（抽出 `ShowerController`），并新增了后台组件（小组件），
> 请重点验证开始 / 停止 / 自动关停 / 消费金额这几条主流程。

---

## v2.1.0

发布日期：2026-09-14

### 版本信息

| 项 | 值 |
|---|---|
| 版本号 | 2.1.0 |
| versionCode | 4 |
| 包名 | `com.hualala.linyu` |
| 构建类型 | Release（R8 混淆 + 签名） |
| GitHub Release | https://github.com/yehu-imei/linyu/releases/tag/v2.1.0 |

### 新增功能

#### 1. 短信验证码登录（通用化）

v1.2.0 中被列为「未完善功能」的短信登录，本版彻底解决。

逆向发现官方 App 发验证码时的 `secret` 参数并非随机值，而是由手机号推导：

```
secret = MD5( 手机号前3位 + 手机号后4位 + "klcx" )
```

由于算法可在本地复现，**任何手机号都能直接收验证码登录**，不再需要抓包或替换硬编码值。实现见 `utils/SignUtils.kt`，`QzxyService.kt` 中的硬编码 secret 已移除。

- 登录页新增「密码登录 / 短信登录」切换
- 60 秒倒计时防重复发送
- 验证码错误、手机号格式错误均有明确提示

#### 2. 内置运行日志查看器

- `utils/AppLogger.kt`：内存环形缓冲（300 条）+ 文件滚动（2MB 自动切分）
- 自动脱敏：手机号、`password`、`loginCode`、`secret`、`telPhone` 等写入前打码
- 自动捕获未处理异常（崩溃堆栈一并落盘）
- 日志查看界面支持级别筛选、复制、清空
- 通过 FileProvider 一键导出/分享日志文件（`xml/file_paths.xml`）

#### 3. 饮水机支持

- 通过 `bigTypeId == 5` 精确识别，缺失时回退到名称关键词判断
- 饮水机使用绿色主题色，图标区分冷水 / 热水
- 设备名格式化：`直饮冷水 江苏大学 F区4栋 6 直饮冷水` → `江苏大学 F区4栋 6`
- 订单状态文案区分：饮水机显示「正在接热水 / 正在接凉水」，洗手台显示「正在洗漱中」，热水器显示「正在沐浴中」
- 账单列表按设备类型着色并显示类型标签

> 作者所在学校无直饮水机，此功能仅完成识别与 UI，实际控制流程未实机验证。

#### 4. 背景装扮

- **主页**与**使用页**各自独立保存一套背景配置，互不影响
- 从背景图自动提取主题色（网易云音乐式），提取结果作为全局强调色
- 支持调整透明度、模糊度、亮度三个参数，实时预览
- 图片按最长边 2560px、JPEG 质量 95 保存，避免糊化
- 启用后状态栏透明，背景图延伸至状态栏；未启用时恢复纯色

#### 5. UI 升级

- **悬浮胶囊导航栏**（`FloatingPillNavBar.kt`）：单一高亮滑块跟随选中项，弹簧阻尼产生果冻回弹；点击无水波纹，仅靠滑块滑动作为反馈
- **圆形揭示主题切换**（`CircularRevealTheme.kt`）：新主题从点击点向外扩散铺开，内容只渲染一次（靠 CompositionLocal 重组而非重建），因此切换不丢状态、不闪烁
- **页面弹性切换**：三大主页之间左右视差滑移 + 淡入淡出
- **液态玻璃卡片**：半透明底色 + 微光描边 + 零阴影，配合自定义背景呈现毛玻璃质感

#### 6. 「我的」页面卡片自定义

- 新增编辑模式，卡片可上下移动排序、隐藏/显示
- 顺序与隐藏状态持久化保存
- 默认顺序：账号信息 → 绑定寝室 → 使用码 → 背景装扮 → 软件更新 → 关于项目 → 运行日志

#### 7. 应用信息 / 软件更新卡片

- 打开卡片即自动检测 GitHub 最新 Release，显示「已是最新」或「发现新版本 vX.Y.Z」
- 更新日志按版本号折叠展示，点击展开
- 展示「当前版本」标记与应用信息

#### 8. 关于项目卡片

- 从 GitHub API 读取仓库信息，显示制作者与 Star 数（失败时回退默认值）
- 一键跳转 GitHub 仓库

### 问题修复

| 问题 | 修复方式 |
|---|---|
| 切到「我的」页面卡顿 | 卡片顺序 / 隐藏状态 / Release 数据 / 仓库信息改为进程级缓存，避免每次切 tab 重复解密与发网络请求 |
| 设备名残留「表」字 | 修正正则处理顺序：优先匹配 `热水[器表]`，避免「热水表-xxx」被截成「表 xxx」 |
| 使用页左上角退出按钮点击无响应 | 调整组件声明顺序，按钮不再被上层全屏 Column 拦截点击 |
| 使用页「已预扣」卡片出现不透明色块 | 补上 `color = Color.Transparent`（Surface 默认使用不透明的 surface 色） |
| 主题切换后跳回首页 | 状态声明移出过渡容器，避免容器内分支重建时丢状态 |
| 主题切换瞬间闪烁 | 遮罩先绘制一帧再启动动画；圆角处残留通过半径 ×1.15 覆盖 |
| 隐藏卡片无法恢复 | 编辑模式下显示全部卡片（含已隐藏项，半透明呈现），可重新显示 |
| 版本号硬编码 | 「我的」页底部版本号改为读取 `BuildConfig.VERSION_NAME` |

### 相比 v1.2.0 的变化一览

| v1.2.0 的状态 | v2.1.0 的状态 |
|---|---|
| 短信登录需要每台设备自己的 secret（未完善） | 已通用化，任何手机号可用 |
| 无日志查看入口 | 内置日志查看器 + 崩溃捕获 + 一键导出 |
| 饮水机仅能识别，UI 未做 | 绿色主题 + 冷热图标 + 名称与文案适配 |
| 无自定义背景 | 主页 / 使用页双套背景 + 主题色提取 |
| 底部导航栏为普通布局 | 悬浮胶囊导航栏 + 果冻滑块 |
| 主题切换生硬 | 圆形揭示动画 |
| 卡片固定顺序 | 可排序、可隐藏 |
| 无更新检测 | 应用内检测 GitHub Release |

### 已知限制

1. **挤号检测是被动的**：需触发网络请求（刷新/操作）才检测到被挤号，打开 App 不操作不会主动发现
2. **实时扣费不可得**：服务器不提供中间消费数据，只能在关闭后查最终金额
3. **消费金额结算延迟**：账单生成有延迟（最长等约 20 秒）
4. **一卡通余额**：易校园 API 有 HMAC-SHA256 native 签名保护，仍只能手动估算
5. **仅适配金华职业技术大学**（projectId=905），其他学校需改 projectId / BLE 过滤名 / MQTT 地址
6. **饮水机未实机验证**：作者所在学校无直饮水机

### 升级提示

- 从 v1.2.0 升级可直接覆盖安装（versionCode 3 → 4，签名一致）
- 若「我的」页面卡片顺序在旧版中调整过，升级后保持不变；从未调整过的会应用新的默认顺序

### 构建说明

Release 构建需跳过 lint（网络下载 lint 依赖失败）：

```powershell
.\gradlew.bat assembleRelease -x lintVitalRelease
```

使用 Android Studio 自带 JBR（JDK 21）：

```powershell
$env:JAVA_HOME="D:\Android Studio\jbr"
.\gradlew.bat assembleRelease -x lintVitalRelease
```

GitHub 推送使用本地 HTTP 代理：

```bash
git config http.proxy http://127.0.0.1:7890
git config https.proxy http://127.0.0.1:7890
git -c http.sslBackend=schannel push origin master
```

---

## v1.2.0

发布日期：2026-08-08

### 新增功能

1. **开阀确认** — 调用 `downRate` 后轮询 `/order/tcpDevice/query/downRateResult` 确认开阀成功才进入洗澡界面，避免设备离线时用户以为开了实际没出水
2. **自动关停倒计时 + 确认弹窗** — 解析 `autoDisConTime` 显示闲置倒计时（时间戳持久化，重启可恢复）；自动关闭时弹出模态确认框（设备名 + 使用时长 + 消费金额）
3. **消费金额结算** — 关阀确认成功后通过账单接口异步轮询（最长 20 秒）获取本次金额，按开阀时间过滤避免读到上一笔
4. **主页扫码绑定设备** — 主页标题栏扫码按钮，扫设备二维码直接查询并弹出详情，无需蓝牙
5. **扫码手电筒** — 扫码界面右上角补光按钮，退出自动关灯
6. **绑定寝室 + 设备筛选** — 「我的」页新增绑定卡片，支持手动输入或从附近设备选择；绑定后主页只显示匹配设备
7. **加密存储** — `loginCode` 等敏感数据改用 `EncryptedSharedPreferences`，不支持时自动降级明文

### 问题修复

| 问题 | 修复 |
|---|---|
| 退出登录闪退 | `PrefsHelper.clear()` 改用逐个 `remove()` 替代 `edit().clear()`（EncryptedSharedPreferences 已知崩溃 bug） |
| 挤号后重新登录重复弹窗 | 挤号场景 `stopShower(skipNetwork = true)` 跳过失效请求 |
| 消费金额显示 0.00 | 账单改为异步轮询，等待服务器结算完成 |
| 结束使用加载卡顿 | 金额查询改为后台异步，不阻塞关闭流程 |
| 自动关停无提示 | 新增确认弹窗替代无声 toast |

---

## v1.1.0

发布日期：2026-07-31

### 新增功能

- 最低 SDK 提升至 API 26
- Kotlin 2.0.21 + AGP 8.13.2 升级

---

## v1.0

发布日期：2026-05-30

### 新增功能

- 初始版本
- 登录/登出、挤号检测
- 蓝牙扫描设备
- 开始/停止洗澡
- MQTT 实时消费推送
- 使用码显示/开关
- 多设备活跃订单管理
- 余额手动估算
- 账单查询
- 深浅主题切换
- 网络异常友好提示
- R8 混淆 + 资源压缩
