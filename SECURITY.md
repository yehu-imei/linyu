# 安全说明

本文说明淋浴（LinYu）在安全方面做了什么、没做什么，以及**你该如何确认自己装的是正版**。

> 一句话版本：**只从下面列出的官方渠道下载，装之前对一下签名指纹。** 别人重新打包的版本可能带后门。

---

## 一、从哪下载

官方只有两个渠道，内容完全一致：

| 渠道 | 地址 | 说明 |
|---|---|---|
| **GitHub**（主仓库） | https://github.com/yehu-imei/linyu/releases | 源码主仓库，发行版在这里发布 |
| **Gitee**（国内镜像） | https://gitee.com/yehu-imei/linyu/releases | 同一份包，国内下载更快 |

**除此之外的任何来源都不是我们发的。** 包括但不限于：

- 第三方应用市场、网盘里别人上传的「淋浴.apk」
- 群里、贴吧里传的安装包
- 打着「去广告版」「破解版」「优化版」旗号的版本

---

## 二、怎么确认你装的是正版

### 这是最重要的一节

**APK 文件的 SHA-256 每个版本都会变**，对不上属正常。真正不变的是**签名证书指纹**——它由签名密钥决定，只要我们用同一把密钥签名，这个值**永远一样**。

### 正版签名指纹

```
证书主体 : CN=Hualala, OU=LinYu, O=Hualala, L=Jinhua, ST=Zhejiang, C=CN

SHA-256 : ccaf8d04aa73a3b3d04a9e0d3abba3d89f8217d411b543f4b563dd3fa2e7e41a
SHA-1   : 33df7934bb508301ff768804f322b961af74752e
MD5     : e30d8bec533ae579f1a6a6698e865c15
```

### 怎么验

**方法一：在手机上用「应用信息」看**

部分手机（小米、华为等）在「设置 → 应用 → 淋浴 → 应用详情」里能看到签名信息。

**方法二：用工具验（推荐，最可靠）**

下载 [apksigner](https://developer.android.com/tools/apksigner)（Android SDK 自带），或者用 [AppChecker](https://github.com/) 这类 App：

```bash
apksigner verify --print-certs 淋浴.apk
```

输出的 `certificate SHA-256 digest` **必须和上面那串完全一致**。

**方法三：比对 SHA-256（限定当前版本）**

如果你只是想确认某个具体版本的包没被篡改，可以对文件做哈希，和发行版页面上的说明比对：

```bash
# Windows
certutil -hashfile linyu-vX.Y.Z.apk SHA256

# Linux / macOS
sha256sum linyu-vX.Y.Z.apk
```

> ⚠️ 注意：这个方法只在**同一版本**内有意义。新版发布后旧值就失效了，所以认签名指纹更靠谱。

---

## 三、为什么不要装别人打包的版本

**这是第三方客户端最大的现实风险，而且和你用不用这个 App 无关——它取决于你从哪里下载。**

### 重新打包能做什么

任何人拿到我们的 APK 后，都可以：

1. 反编译，插一段代码
2. 重新签名
3. 发到各种渠道

**技术上没有任何门槛**，而且改过的包**外观、功能、界面可以做到和正版一模一样**。

### 插进去的代码会干什么

常见的几种：

| 手段 | 后果 |
|---|---|
| 在登录页加一行上报 | **你的账号密码直接发到攻击者服务器** |
| 替换内置的更新地址 | 让你一直装他们的版本 |
| 加广告 SDK / 引流 | 你的设备被拿去刷量 |
| 加远控后门 | 手机被远程控制 |

第一种最常见，也最致命。

### 为什么我们的包特别容易被盯上

这个 App 的用户是**在校学生**，账号往往和其他校园系统共用一套密码。一个改过的安装包，可能一次收到成百上千个真实的校园账号。

**所以：不要把安装包转发给别人，也不要装别人转发的安装包。** 要分享就直接发官方链接。

### 我们能做的防御

所有发行版都用**同一把密钥签名**（指纹见上）。所以只要你验一次签名，就能确定这个包出自我们这里——**重新打包的人没有这把密钥，签名一定对不上。**

---

## 四、我们的安全设计

### 1. 没有后端服务器 —— 这是最根本的一条

淋浴**没有自己的服务器**。你的账号密码从手机**直接发到趣智校园官方接口**，中间不经过任何我们控制的机器。

意味着：

- 我们**看不到**你的密码、手机号、消费记录
- 我们**存不下**你的任何数据
- 就算我们的仓库被黑，攻击者也拿不到用户数据——**因为根本没有**

这也是为什么本 App 能做到「只装一个 APK 就能用」：没有服务端要维护。

### 2. 密码不落盘

登录时密码只在内存里存在几毫秒：

```
输入明文 → MD5 后取 10 位 → 发出去 → 内存里的明文立即丢弃
```

`PrefsHelper` 里**没有任何 password 字段**，密码不会被写进任何本地存储。重装 App、清除数据，都不会留下密码痕迹。

### 3. 全站强制 HTTPS

`network_security_config.xml` 里默认**禁止一切明文流量**：

```xml
<base-config cleartextTrafficPermitted="false">
```

唯一的例外是趣智校园的 MQTT 服务器（原因见下方「已知暴露面」）。

### 4. 敏感数据加密存储

`loginCode`、`userId` 等会话凭证，以及**姓名、学号、一卡通余额**（v3.0.0 起会从接口拉取），
全部存在 **EncryptedSharedPreferences** 里，由 Android Keystore 托管密钥。
**手机被 root 之前，这些数据是解不开的。**

> 这些都只是为了**显示**——不显示姓名学号、不显示余额，App 其余功能照常可用。

### 5. 日志自动脱敏

App 内置日志导出功能（方便排查问题），但**写入前会自动打码**：

| 字段 | 处理 |
|---|---|
| 手机号 | 保留前 3 后 4，如 `191****2082` |
| password / loginCode / secret / smsCode | 整个值替换成 `***` |

而且 **HTTP 访问日志只记录路径，不记录查询串**——所以 `loginCode` 这类会出现在 URL 里的凭证**不会进日志文件**。你导出日志发给别人，不会有敏感信息。

### 6. Release 版仍保留必要的网络排查日志

`HttpLoggingInterceptor` 的 BODY 级别完整日志**只在 Debug 构建里存在**，正式发布的包中不包含。

但 Release 版仍会通过项目自己的日志记录接口路径、请求耗时，以及最多 300 个字符的请求片段和 400 个字符的响应片段。这些片段写入前会经过上面的脱敏规则，但脱敏只覆盖已知字段和格式，不能保证识别所有敏感内容。**导出分享前请自行检查，并只发送给可信的维护者。**

### 7. 代码混淆与资源压缩

Release 构建启用 **R8 Full Mode**，代码标识符全部混淆、无用代码剔除，增大逆向和篡改的成本。

### 8. 更新包校验

应用内更新只从**固定仓库**读取 Release 信息，下载地址默认走国内镜像，可自行切回。

下载完成后会先核对附件长度；如果 GitHub Release API 提供 `sha256` 摘要，还会校验文件 SHA-256。Gitee 本身不提供摘要，而且国内网络可能无法访问 GitHub API，因此摘要不可用时不会阻止下载。

无论摘要是否可用，调起安装器前都会确认更新包的包名与当前应用一致，并比较双方签名证书的 SHA-256 摘要。包名、签名不一致或签名无法读取时会直接阻止安装。因此 Gitee 用户在 GitHub 摘要不可用时仍能更新，但签名校验不会降级跳过。

---

## 五、已知暴露面（诚实披露）

下面这些是**真实存在的**，我们写出来而不是藏着——因为其中大部分**不是我们能在 App 端解决的**。

### 1. 会话令牌出现在 URL 查询串里

趣智校园的官方 API 要求 GET 请求把认证参数**放在 URL 里**：

```
GET /account/wallet?loginCode=xxx&userId=xxx&telephone=xxx&...
```

这意味着 URL 可能被**代理日志、服务器 access.log、CDN 日志**记录。

**为什么改不了**：这是官方接口的协议设计，第三方客户端只能遵循。改成 POST 服务端不认。

**影响**：需要攻击者能接触到上述日志才有意义，普通用户不受影响。

### 2. MQTT 实时推送走明文

```kotlin
tcp://47.107.37.60:1883    // 不是 ssl://
```

趣智校园的 MQTT 服务器**不支持 TLS**，所以推送内容（订单状态、消费金额）在传输中未加密。同一 WiFi 下的攻击者理论上可以嗅探。

**影响**：只会泄露「谁在什么时候用了多少水」这类信息，**不含账号密码**。

### 3. 未启用证书固定（Certificate Pinning）

App 没有做证书固定，所以**在设备上安装了自定义 CA 证书的人**可以解密 HTTPS 流量。

**为什么不做**：证书固定会在服务端更换证书时导致**全体用户无法连接**，需要紧急发版；而攻击者也可以反编译去掉固定逻辑。收益与风险不成比例。

**影响**：仅对「已经能在你手机上装证书」的攻击者有效——正常情况下这个人需要先拿到你的设备。

### 4. 密码使用 MD5 处理后传输

趣智校园官方协议要求：`MD5(明文) → 取后 10 位 → 转大写`。

**MD5 已经不安全**，攻击者拿到摘要后可以暴力枚举。

**为什么改不了**：服务端校验的就是这个格式，客户端改不了。

**缓解**：这也是为什么我们**不存密码**——摘要只有在传输中短暂存在。

### 5. App 能发起一卡通代扣（会真的扣钱）

v3.0.1 起支持「请求代扣」：如果账单因为余额不足或 12 点后结算失败留在待扣状态，
可以在账单上手动补扣。**这是直接操作你校园卡余额的接口，点下去钱就扣了。**

我们做的防护：

- 只对**服务端返回的未支付账单**显示按钮，不猜哪些账单该扣
- 点按钮先弹**确认框**，显示金额和笔数
- 结果分**三态**：服务端明确拒绝才叫失败；**请求超时或中断归为「结果未知」**，
  这时不会让你重试，而是重新查一遍服务端状态——因为那笔**可能已经扣成功了**，
  再点一次就是扣两次
- 金额解析不出来时**直接禁用确认按钮**，不让你对着一个可能是假的数字点头

> 但说到底，**这是你的钱**。如果不放心，可以只用这个 App 开关水阀，代扣去官方 App 做。

### 6. MQTT 服务器地址是硬编码的

`47.107.37.60:1883` 写死在代码里。这不算安全漏洞（本来就是公开服务），但如果你要适配别的学校，需要自己改。

---

## 六、你可以做的

### 必做

1. **只从 GitHub / Gitee 官方发行版下载**，不要用别人转发的安装包
2. **验一次签名指纹**（见第二节），确认是正版
3. **不要把安装包转发给别人** —— 要分享就发官方链接

### 建议

4. **用系统自带的密码管理器**保存密码，不要用第三方输入法记住
5. **不要 root 手机**，不要安装来源不明的 CA 证书（有些抓包工具要求装证书，装了就给了别人解密你流量的能力）
6. **定期检查「设置 → 应用 → 淋浴 → 权限」**，正常只需要：蓝牙、相机（扫码）、通知、安装未知应用。**不需要通讯录、短信、位置**（Android 12 以上）

### 不需要做的

- ❌ 不需要担心「App 偷传数据」——没有服务器可传
- ❌ 不需要定期改密码给这个 App 用——它不存密码

---

## 七、报告安全问题

发现安全漏洞，请通过以下方式联系：

- **邮箱**：2276391153@qq.com
- **GitHub Issue**：https://github.com/yehu-imei/linyu/issues
- **Gitee Issue**：https://gitee.com/yehu-imei/linyu/issues

**请勿公开披露**，给我们一点修复时间。收到报告后我们会：

1. 尽快确认问题
2. 在下一个版本中修复
3. 修复后更新本文档，并在更新日志中致谢（如果你愿意留名）

---

## 八、免责声明

本项目是**第三方非官方客户端**，与趣智校园官方无关。

使用本软件即表示你理解：App 会将你的登录凭据发送至趣智校园官方接口，这一过程不受我们控制。请自行评估是否使用。

---

*最后更新：2026-09-18*
